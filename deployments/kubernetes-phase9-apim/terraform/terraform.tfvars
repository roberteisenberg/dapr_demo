# Copy to terraform.tfvars and fill in values
# cp terraform.tfvars.example terraform.tfvars

resource_group_name  = "rg-dapr-demo"
location             = "eastus"
apim_publisher_email = "your-email@example.com"
