server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Serve static SPA files
    # API calls to /v1.0/invoke/* are handled by the Ingress controller
    location / {
        try_files $uri $uri/ /index.html;
    }
}
